# Theodore Sinclair 公开模板约定

## 产品定位

- 本仓库是 Mine 去私人化后的公开版本，面向所有人开放，供使用者部署或 fork 后养成属于自己的 Theodore Bot。
- 它与 Mine、Theo 共享一部分核心能力，但必须保持可公开、可移植和可配置。
- 不得写入西柚的私人身份、恋爱细节、记忆、服务器/频道 ID、数据库内容、密钥或 Mine 专用提示词。

## 同步原则

- Mine 通常是功能最前沿的来源，但不要自动同步。先由用户决定要同步哪些功能。
- 从 Mine 同步恋爱相关能力时，应转化为使用者自行配置的伴侣资料和本地/环境配置，不预置西柚的资料。
- 从 Theo 同步公共功能时，也要检查模板使用者是否能通过文档和配置独立部署，避免依赖西柚的服务器结构。

## 开发约束

- 涉及删除任何现有功能、命令、配置、数据、兼容性或文档时，必须先说明具体范围并得到用户明确同意。需求含糊或存在多种解释时先提问。
- 不记录或提交真实 Token、API Key、数据库凭据或任何部署者的私人资料。
- 新增配置时同步更新示例配置和 README；默认值应安全，缺失配置时给出清楚错误。
- 修改后检查 Python 语法、导入、命令注册、数据库初始化、数据隔离、全新部署流程及 Windows/Linux 托管兼容性。

